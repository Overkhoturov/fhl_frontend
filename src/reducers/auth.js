// import * as actionTypes from '../action-types/auth';

const initialState = {
  isLogedIn: false,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
