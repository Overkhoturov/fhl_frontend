// import { takeLeading } from 'redux-saga/effects';

// import * as auth from './auth';

function* rootSaga() {
  // yield takeLeading(auth.types.AUDIO_CLOSE_PLAYER_SAGA, auth.loginSaga);
}

export default rootSaga;
